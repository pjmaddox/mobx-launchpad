import React from 'react';
import ExampleStore from "../stores/ExampleStore";

describe("ExampleStore", () => {
    describe("AddObjectToArray function", () => {
        it("should result in an object being added the ", () => {

        });
    });
    describe("ModifySomeString", () => {
        it("should change the value of SomeString to the parameter's value", () => {
            let 
        });
    });
});